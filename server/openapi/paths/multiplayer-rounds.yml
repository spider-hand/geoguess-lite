post:
  description: Create multiplayer rounds
  tags:
    - multiplayer-rounds
  operationId: createMultiplayerRounds
  requestBody:
    $ref: ../components/requestBodies/multiplayer-rounds-post.yml
  responses:
    "200":
      description: Multiplayer rounds created successfully
      content:
        application/json:
          schema:
            type: object
            properties:
              message:
                type: string
                example: Multiplayer rounds created successfully
    default:
      description: Error response
      content:
        application/json:
          schema:
            $ref: ../components/schemas/error-response.yml
  