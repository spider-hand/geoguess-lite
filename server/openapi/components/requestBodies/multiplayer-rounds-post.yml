required: true
content:
  application/json:
    schema:
      type: object
      properties:
        room_id:
          type: string
        only_panorama:
          type: boolean
      required:
        - room_id
        - only_panorama
